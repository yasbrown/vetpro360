<div class="container">
  <h1>Edit note</h1>

  <%= form_with model: @note do |f| %>
    <%= f.text_field :content %>
    <%= f.submit %>
  <% end %>

  <%= link_to "Back", history_animal_path(@animal) %>
</div>
