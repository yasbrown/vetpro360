<%= link_to "See all owners", owner_attributes_path %>

<hr>

<div class="container">
  <h1>Appointments available today</h1>

  <% @available_slots.each do |slot| %>
    <div class="flex">
      <p>start time: <%= slot.start_time.strftime("%H:%M:%S") %></p>
      <p>end time:<%= slot.end_time.strftime("%H:%M:%S") %></p>
      <%= link_to "Book this appointment", new_slot_appointment_path(slot) %>
    </div>
  <% end %>

  <hr>

  <h1>Booked appointments</h1>

  <% @booked_appointments.each do |appointment| %>
    <div class="border my-2">
      <p>Animal:<%= appointment.animal.name %></p>
      <p>Vet: <%= appointment.vet_attribute.name %></p>
      <p>Start time: <%= appointment.start_time.strftime("%H:%M:%S") %></p>
      <p>End time: <%= appointment.end_time.strftime("%H:%M:%S") %></p>
      <p>Reason for appointment: <%= appointment.reason_for_appointment %></p>
      <p><%= link_to "See details", appointment_path(appointment) %></p>
      <p><%= link_to "Cancel appointment", destroy_appointment_path(appointment), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
    </div>
  <% end %>


  <hr>

  <div class="container text-center">
    <div class="row row-cols-5">
      <div class="col border"></div>
      <div class="col border">Vet1</div>
      <div class="col border">Vet2</div>
      <div class="col border">Vet3</div>
      <div class="col border">Nurse</div>

      <div class="col border">9:00</div>
      <div class="col border"></div>
      <div class="col border"></div>
      <div class="col border"></div>
      <div class="col border"></div>

      <div class="col border">9:15</div>
      <div class="col border"></div>
      <div class="col border"></div>
      <div class="col border"></div>
      <div class="col border"></div>

      <div class="col border">9:30</div>
      <div class="col border"></div>
      <div class="col border"></div>
      <div class="col border"></div>
      <div class="col border"></div>

      <div class="col border">9:45</div>
      <div class="col border"></div>
      <div class="col border"></div>
      <div class="col border"></div>
      <div class="col border"></div>

      <div class="col border">10:00</div>
      <div class="col border"></div>
      <div class="col border"></div>
      <div class="col border"></div>
      <div class="col border"></div>

    </div>
  </div>
</div>
